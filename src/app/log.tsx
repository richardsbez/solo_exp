import { HudPlaceholderScreen } from '@/components/hud-placeholder-screen';

// Nome de rota provisório: o ícone (3 linhas centralizadas) veio do
// último design do Figma, mas ainda não veio o nome/conteúdo dessa aba.
// Troque "log.tsx" pelo nome definitivo (e ajuste a rota em
// hud-bottom-nav.tsx) quando o design chegar.
export default function LogScreen() {
  return <HudPlaceholderScreen title="LOG" />;
}
