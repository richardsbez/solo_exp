import { HudPlaceholderScreen } from '@/components/hud-placeholder-screen';

// Menu de outras funções (inventário, loja, config...) a serem
// adicionadas no futuro. Era o placeholder "log" antes.
export default function MenuScreen() {
  return <HudPlaceholderScreen title="MENU" />;
}
