import { HudPlaceholderScreen } from '@/components/hud-placeholder-screen';

// Histórico de notificações do Sistema (era chamada de "chat" antes).
export default function NotificationsScreen() {
  return <HudPlaceholderScreen title="NOTIFICATIONS" />;
}
