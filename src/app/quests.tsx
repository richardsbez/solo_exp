import { HudPlaceholderScreen } from '@/components/hud-placeholder-screen';

// Missões Principal e Secundária (main/side quests). O ícone na barra
// continua sendo o calendário — é só o nome/conteúdo da aba que mudou.
export default function QuestsScreen() {
  return <HudPlaceholderScreen title="QUESTS" />;
}
