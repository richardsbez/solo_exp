import { TabPlaceholder } from '@/components/tab-placeholder';

// Missões Principal e Secundária (main/side quests). O ícone na barra é
// o calendário, mas a tela não tem nada a ver com data.
export function QuestsScreen() {
  return <TabPlaceholder title="QUESTS" />;
}
